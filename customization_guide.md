# 🎨 PRODUCT CARD - QUICK CUSTOMIZATION GUIDE\n\n## 🎯 Most Common Changes\n\n### 1. Change Primary Color (Green)\n\n**Find** in `shop.css`:\n```css\n:root {\n  --accent: #00B894;  ← Change this\n}\n```\n\n**Examples:**\n- Blue: `#2563eb`\n- Purple: `#9333ea`\n- Red: `#ef4444`\n- Pink: `#ec4899`\n- Orange: `#f97316`\n\n✅ All green elements (buttons, badges, text) will change automatically\n\n---\n\n### 2. Change Card Border Radius\n\n**Find** in `shop.css`:\n```css\n:root {\n  --radius: 14px;     ← Change this (cards)\n  --radius-sm: 8px;   ← Change this (buttons)\n}\n```\n\n**Examples:**\n- Very rounded: `--radius: 24px`\n- Square: `--radius: 0px`\n- Pill-shaped: `--radius: 50px`\n- Medium: `--radius: 12px`\n\n---\n\n### 3. Change Product Grid Columns\n\n**For Desktop (5 columns instead of 4):**\n```css\n.product-grid {\n  grid-template-columns: repeat(5, 1fr);  ← Change 4 to 5\n  gap: 20px;\n}\n```\n\n**For Tablet (4 columns instead of 3):**\n```css\n@media (max-width: 1024px) {\n  .product-grid {\n    grid-template-columns: repeat(4, 1fr);  ← Change 3 to 4\n    gap: 18px;\n  }\n}\n```\n\n**For Mobile (1 column instead of 2):**\n```css\n@media (max-width: 640px) {\n  .product-grid {\n    grid-template-columns: repeat(1, 1fr);  ← Change 2 to 1\n    gap: 12px;\n  }\n}\n```\n\n---\n\n### 4. Change Image Height\n\n**Find** in `shop.css`:\n```css\n.product-media {\n  height: 240px;  ← Change this\n}\n```\n\n**Examples:**\n- Shorter: `height: 180px`\n- Taller: `height: 300px`\n- Square: `height: 280px` (if card width is 280px)\n\n**Also update for tablets:**\n```css\n@media (max-width: 1024px) {\n  .product-media {\n    height: 220px;  ← Adjust for tablet\n  }\n}\n```\n\n---\n\n### 5. Change Button Text\n\n**In `shop.js`**, find the button HTML:\n```javascript\n<button class=\"add-btn\" data-id=\"${product.id}\">\n  <i class=\"fa-solid fa-shopping-cart\"></i>\n  <span>Add to Cart</span>  ← Change this text\n</button>\n```\n\n**Change to:**\n- `Buy Now`\n- `Purchase`\n- `Get It`\n- `Order Now`\n\n---\n\n### 6. Change Shadow Depth\n\n**Find** in `shop.css`:\n```css\n:root {\n  --shadow: 0 10px 30px rgba(2, 6, 23, 0.06);    ← Normal\n  --shadow-md: 0 20px 50px rgba(2, 6, 23, 0.1);  ← Hover\n  --shadow-lg: 0 25px 60px rgba(2, 6, 23, 0.15); ← Modal\n}\n```\n\n**For subtle shadows:**\n```css\n--shadow: 0 4px 12px rgba(0, 0, 0, 0.08);\n--shadow-md: 0 8px 20px rgba(0, 0, 0, 0.12);\n```\n\n**For dramatic shadows:**\n```css\n--shadow: 0 12px 36px rgba(0, 0, 0, 0.12);\n--shadow-md: 0 24px 60px rgba(0, 0, 0, 0.18);\n```\n\n---\n\n### 7. Change Discount Badge Color\n\n**Find** in `shop.css`:\n```css\n.product-badge--discount {\n  background: linear-gradient(135deg, var(--danger) 0%, #ff5a23 100%);  ← Change colors\n  color: white;  ← Change text color\n}\n```\n\n**Examples:**\n- Blue gradient: `135deg, #2563eb 0%, #1d4ed8 100%`\n- Orange gradient: `135deg, #f97316 0%, #ea580c 100%`\n- Purple gradient: `135deg, #9333ea 0%, #7e22ce 100%`\n- Solid color: `background: #ff6b35;` (remove gradient)\n\n---\n\n### 8. Change Star Rating Color\n\n**Find** in `shop.css`:\n```css\n.product-rating-stars {\n  color: #ffc107;  ← Change this\n}\n```\n\n**Examples:**\n- Red: `#ef4444`\n- Green: `#10b981`\n- Blue: `#3b82f6`\n- Purple: `#8b5cf6`\n- Gold (original): `#ffc107`\n\n---\n\n### 9. Change Card Padding\n\n**Find** in `shop.css`:\n```css\n.product-body {\n  padding: 16px;  ← Change this\n}\n```\n\n**Examples:**\n- Spacious: `padding: 20px`\n- Compact: `padding: 12px`\n- Very spacious: `padding: 24px`\n\n---\n\n### 10. Hide/Show Elements\n\n**To hide category badge:**\n```css\n.product-category {\n  display: none;\n}\n```\n\n**To hide rating:**\n```css\n.product-rating {\n  display: none;\n}\n```\n\n**To hide description:**\n```css\n.product-desc {\n  display: none;\n}\n```\n\n**To hide old price:**\n```css\n.product-price-original {\n  display: none;\n}\n```\n\n---\n\n## 🔧 Adding New Features\n\n### Add \"Out of Stock\" Badge\n\n**In shop.js**, update createProductCard:\n```javascript\nlet stockBadge = '';\nif (!product.stock) {\n  stockBadge = `<div class=\"product-badge product-badge--stock\">Out of Stock</div>`;\n}\n\n// In HTML template:\narticle.innerHTML = `\n  <div class=\"product-media\">\n    ${discountBadge}\n    ${stockBadge}  ← Add this\n    ...\n```\n\n**In shop.css**, add styling:\n```css\n.product-badge--stock {\n  background: #64748b;\n  color: white;\n  top: 12px;\n  left: 12px;  ← Left side\n}\n```\n\n---\n\n### Add \"Best Seller\" Badge\n\n**In shop.js**, update product data:\n```javascript\nconst products = [\n  {\n    id: 1,\n    name: '...',\n    bestseller: true,  ← Add this\n    ...\n  }\n];\n```\n\n**In createProductCard:**\n```javascript\nlet sellerBadge = '';\nif (product.bestseller) {\n  sellerBadge = `<div class=\"product-badge product-badge--bestseller\">⭐ Best Seller</div>`;\n}\n```\n\n**In HTML:**\n```javascript\narticle.innerHTML = `\n  <div class=\"product-media\">\n    ${discountBadge}\n    ${sellerBadge}  ← Add this\n    ...\n```\n\n**In shop.css:**\n```css\n.product-badge--bestseller {\n  background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);\n  color: #1f2937;\n  top: 12px;\n  left: 12px;\n}\n```\n\n---\n\n### Change Hover Animation Speed\n\n**Find** in `shop.css`:\n```css\n:root {\n  --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);  ← Change 0.3s\n  --transition-fast: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\n}\n```\n\n**Examples:**\n- Faster: `0.2s`\n- Slower: `0.5s`\n- Instant: `0.1s`\n- Slow: `0.8s`\n\n---\n\n### Change Hover Lift Distance\n\n**Find** in `shop.css`:\n```css\n.product-card:hover {\n  transform: translateY(-6px);  ← Change -6px\n}\n```\n\n**Examples:**\n- Less lift: `translateY(-3px)`\n- More lift: `translateY(-10px)`\n- No lift: `translateY(0px)`\n- Down: `translateY(6px)`\n\n---\n\n### Change Image Zoom Amount\n\n**Find** in `shop.css`:\n```css\n.product-card:hover .product-image {\n  transform: scale(1.06);  ← Change 1.06\n}\n```\n\n**Examples:**\n- Subtle zoom: `scale(1.03)`\n- More zoom: `scale(1.12)`\n- Heavy zoom: `scale(1.2)`\n- No zoom: `scale(1)`\n\n---\n\n## 📱 Responsive Breakpoint Customization\n\n### Change Tablet Breakpoint\n\n**Find** in `shop.css`:\n```css\n@media (max-width: 1024px) {  ← Change this\n  .product-grid {\n    grid-template-columns: repeat(3, 1fr);\n  }\n}\n```\n\n**Change to:**\n- `@media (max-width: 1200px)` - earlier breakpoint\n- `@media (max-width: 900px)` - later breakpoint\n\n---\n\n### Change Mobile Breakpoint\n\n**Find** in `shop.css`:\n```css\n@media (max-width: 640px) {  ← Change this\n  .product-grid {\n    grid-template-columns: repeat(2, 1fr);\n  }\n}\n```\n\n**Change to:**\n- `@media (max-width: 768px)` - larger screen\n- `@media (max-width: 512px)` - smaller screen\n\n---\n\n## 🎯 Pro Tips\n\n✅ **Always test on multiple devices** after making changes\n✅ **Use DevTools to preview responsive changes** (F12 → Device Toolbar)\n✅ **Keep color variables consistent** for easy theme changes\n✅ **Document your changes** for future reference\n✅ **Make one change at a time** to see the effect clearly\n✅ **Use CSS comments** to mark customized sections\n✅ **Test all breakpoints** (1200px, 1024px, 768px, 640px, 480px)\n✅ **Check color contrast** for accessibility\n✅ **Verify touch targets** are at least 48px on mobile\n✅ **Test with real products** to see actual content\n\n---\n\n## 🚀 Quick Testing Checklist\n\nAfter each customization:\n- [ ] Desktop view looks correct\n- [ ] Tablet view looks correct\n- [ ] Mobile view looks correct\n- [ ] Hover effects work smoothly\n- [ ] Buttons are clickable\n- [ ] Images display properly\n- [ ] Text is readable\n- [ ] No overlapping elements\n- [ ] Color contrast is good\n- [ ] Performance is acceptable\n\n---\n\n*Last Updated: January 14, 2026*\n